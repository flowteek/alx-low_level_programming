#include <stdio.h>

/**
 * main-print the phrase without any error
 * return: 0 if existed, non-zero if not exited
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
