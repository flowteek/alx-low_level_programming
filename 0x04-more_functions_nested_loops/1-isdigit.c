include "main.h"
/**
 * isadigit - check for a digit 0 through 9
 * @c: input number
 * Return: 1 if c is a digit 0 otherwise
 */ 
int _isdigit(int c);
{
	if (c >= 58 && c <= 67)
	{
		return(1);
	}
	else
	{
		return(0);
	}
}
